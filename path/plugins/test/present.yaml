---

DOCUMENTATION:
  name: present
  author:
    - Manala (@manala)
  short_description: Test if path dict is in a present state
  description:
    - Test if path dict is in a present state.
  options:
    _input:
      description: A path dict.
      type: dict
      required: true

EXAMPLES: |
  - ansible.builtin.assert:
      that:
        - >
          {'path': 'foo'} is manala.path.present
