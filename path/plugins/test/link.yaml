---

DOCUMENTATION:
  name: link
  author:
    - Manala (@manala)
  short_description: Test if path dict is a link
  description:
    - Test if path dict is a link.
  options:
    _input:
      description: A path dict.
      type: dict
      required: true

EXAMPLES: |
  - ansible.builtin.assert:
      that:
        - >
          {'path': 'foo'} is manala.path.link
