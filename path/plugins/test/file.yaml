---

DOCUMENTATION:
  name: file
  author:
    - Manala (@manala)
  short_description: Test if path dict is a file
  description:
    - Test if path dict is a file.
  options:
    _input:
      description: A path dict.
      type: dict
      required: true

EXAMPLES: |
  - ansible.builtin.assert:
      that:
        - >
          {'path': 'foo'} is manala.path.file
